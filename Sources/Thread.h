#ifndef _THREAD_H
#define _THREAD_H 1

namespace ProjectEagle
{
	/*class Thread
	{
	private:
		HANDLE threadHandle;
		DWORD ID;
		bool isRunning;

	public:
		Thread();
		~Thread(){};
		void Initialize();
		virtual DWORD threadFunction() = 0;
		void End();
	};

	class ThreadMonitor
	{
	private:
		HANDLE mutexHandle;
	public:
		ThreadMonitor();
		~ThreadMonitor();

		void mutexOn() const;
		void mutexOff() const;
	};*/
};

#endif